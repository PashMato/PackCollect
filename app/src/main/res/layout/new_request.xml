<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Package Location -->
        <EditText
            android:id="@+id/package_location_te"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:hint="Package Location"
            android:inputType="text"
            android:padding="8dp"
            android:gravity="start|center_vertical"/>

        <Space
            android:layout_width="match_parent"
            android:layout_height="12dp"/>

        <!-- Package Data / Contents -->
        <EditText
            android:id="@+id/package_data_te"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:hint="Package Data / Contents"
            android:inputType="text"
            android:padding="8dp"
            android:gravity="start|center_vertical"/>

        <Space
            android:layout_width="match_parent"
            android:layout_height="12dp"/>

        <!-- Additional Details -->
        <EditText
            android:id="@+id/additional_details_te"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:hint="Additional Details"
            android:inputType="textMultiLine"
            android:gravity="start|top"
            android:padding="8dp"
            android:scrollbars="vertical"/>

        <Space
            android:layout_width="match_parent"
            android:layout_height="20dp"/>

        <!-- Submit Button -->
        <Button
            android:id="@+id/create_request_bt"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:text="Create Request"/>

    </LinearLayout>
</ScrollView>
